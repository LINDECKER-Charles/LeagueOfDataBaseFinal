symfonycasts_reset_password:
    request_password_repository: App\Repository\ResetPasswordRequestRepository
    # Reset link valid for one hour; one request per hour per user (anti-abuse).
    # Expired rows are garbage-collected when new requests come in.
    lifetime: 3600
    throttle_limit: 3600
    enable_garbage_collection: true
