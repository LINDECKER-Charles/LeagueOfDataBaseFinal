twig:
    file_name_pattern: '*.twig'
    globals:
        # Current application release — rendered in the navbar and on /changelog.
        app_version: '%app.version%'

when@test:
    twig:
        strict_variables: true
