{% extends 'admin/shell/_base.html.twig' %}
{% import 'admin/shell/_ui.html.twig' as ui %}

{% block title %}Audience · Admin · LODB{% endblock %}

{% block admin_body %}
    <div class="page-toolbar">
        {{ ui.page_head('Analytics', 'Audience',
            'Qui consulte le site — géographie, appareils et sources.') }}
        {{ ui.range_bar(range, ranges, 'admin_audience') }}
    </div>

    {{ ui.panel('audience', { range: range }, panels['audience']|default(null), 'cards') }}
{% endblock %}
