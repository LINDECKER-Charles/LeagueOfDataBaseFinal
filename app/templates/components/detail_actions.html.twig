{# Load-time marker: flags this render as a detail page and carries the server
   generation time (ms). The persistent LoadTimeBadge island reads it to display
   the load-time chip; it is absent on list/home pages, so the chip self-hides. #}
<div data-load-timing data-server-ms="{{ page_render_ms() }}" hidden></div>
