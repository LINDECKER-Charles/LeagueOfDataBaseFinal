; Development overrides (mounted over the prod config by compose.override.yaml)
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
; Enable APCu in CLI too, so the warmup command shares the dev ddragon.cache pool.
apc.enable_cli = 1
